services:
  api:
    environment:
      APP_ENV: development
    volumes:
      - .:/app
    command: >
      sh -c "alembic upgrade head &&
      exec uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload"

  frontend:
    ports:
      - "5173:80"
